[tool.black]
line-length = 100
target-version = ["py310"]

[tool.ruff]
line-length = 100
select = ["E", "F", "I", "UP"]
ignore = ["E203"]

[tool.ruff.per-file-ignores]
"src/__init__.py" = ["F401"]

[tool.isort]
profile = "black"
line_length = 100
